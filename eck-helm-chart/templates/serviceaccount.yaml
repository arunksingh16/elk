{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "eck-helm-chart.serviceAccountName" . }}
  labels:
{{ include "eck-helm-chart.labels" . | indent 4 }}
{{- end -}}
