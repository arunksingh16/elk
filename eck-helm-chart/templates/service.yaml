apiVersion: v1
kind: Service
metadata:
  name: {{ include "eck-helm-chart.fullname" . }}
  labels:
{{ include "eck-helm-chart.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "eck-helm-chart.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
